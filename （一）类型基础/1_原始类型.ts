/** 字符型 */
const str: string = "hello typescript";

/** 数值型 */
const num: number = 1000;

/** 布尔型 */
const flag: boolean = false;

/** null（表示的是一个有意义的具体类型值） */
const nu: null = null;

/** undefined（表示的是一个有意义的具体类型值） */
const un: undefined = undefined;

/** Symbol */
const sybolValue: symbol = Symbol("louhaojie");

/** Bigint */
const bigintValue1: bigint = 9007199254740991n; // 科学计数法
const bigintValue2: bigint = BigInt(9007199254740991);
